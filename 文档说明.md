我来为你创建一个完整的 Skill，用于指导 AI 助手实现这个物流批量下单系统。

```markdown
# Skill: 物流批量下单系统开发

## 技能描述
此 Skill 用于指导开发一个支持多模板 Excel 导入的物流批量下单 Web 应用，包含模板管理、数据预览编辑、批量提交和历史查询四大模块。

## 适用场景
- 用户需要上传不同格式的 Excel 文件进行批量物流下单
- Excel 模板格式多样（列名不同、列序不同、含合并单元格）
- 需要自动识别模板并记忆映射关系
- 需要在线编辑、校验数据后再提交

---

## 一、技术栈要求

| 类别 | 技术选型 | 说明 |
|------|----------|------|
| 框架 | Next.js 14+ (App Router) | 全栈应用，API Routes 处理后端 |
| 语言 | TypeScript | 类型安全 |
| UI 组件 | shadcn/ui | 现代、可定制 |
| 表格 | TanStack Table + React Data Grid | 支持单元格编辑、虚拟滚动 |
| Excel 解析 | SheetJS (xlsx) | 解析 Excel 文件 |
| 拖拽上传 | react-dropzone | 拖拽和点击上传 |
| 状态管理 | Zustand / TanStack Query | 客户端状态 + 服务端缓存 |
| 校验 | Zod | 数据校验 schema |
| 数据库 | Supabase / Neon | PostgreSQL，支持实时查询 |
| ORM | Prisma / Drizzle ORM | 数据库操作 |
| 部署 | Vercel | 一键部署 |

---

## 二、项目结构

```
logistics-bulk-import/
├── src/
│   ├── app/
│   │   ├── (auth)/          # 认证相关（可选）
│   │   ├── (main)/
│   │   │   ├── page.tsx     # 主页面 - 导入模块
│   │   │   ├── preview/
│   │   │   │   └── page.tsx # 预览编辑页面
│   │   │   └── history/
│   │   │       └── page.tsx # 历史运单列表页面
│   │   ├── api/
│   │   │   ├── upload/
│   │   │   │   └── route.ts # 文件解析接口
│   │   │   ├── shipments/
│   │   │   │   └── route.ts # 运单 CRUD
│   │   │   ├── templates/
│   │   │   │   └── route.ts # 模板映射管理
│   │   │   └── validate/
│   │   │       └── route.ts # 数据校验接口
│   │   └── layout.tsx
│   ├── components/
│   │   ├── upload/
│   │   │   ├── DragDropUpload.tsx
│   │   │   ├── TemplateMapping.tsx
│   │   │   └── ImportProgress.tsx
│   │   ├── preview/
│   │   │   ├── EditableTable.tsx
│   │   │   ├── CellEditor.tsx
│   │   │   ├── ErrorTooltip.tsx
│   │   │   └── ValidationSummary.tsx
│   │   ├── history/
│   │   │   ├── ShipmentList.tsx
│   │   │   ├── SearchFilter.tsx
│   │   │   └── Pagination.tsx
│   │   └── common/
│   │       └── ProgressBar.tsx
│   ├── lib/
│   │   ├── excel/
│   │   │   ├── parser.ts      # Excel 解析
│   │   │   ├── templateDetector.ts  # 模板识别
│   │   │   ├── columnMapper.ts     # 列映射
│   │   │   └── exporter.ts        # 导出 Excel
│   │   ├── validation/
│   │   │   ├── shipmentSchema.ts   # Zod schema
│   │   │   └── validator.ts       # 批量校验
│   │   ├── db/
│   │   │   └── client.ts       # 数据库客户端
│   │   └── utils/
│   │       └── storage.ts       # localStorage 存储
│   ├── hooks/
│   │   ├── useTemplateMapping.ts
│   │   ├── useShipmentValidation.ts
│   │   └── useBatchSubmit.ts
│   ├── types/
│   │   ├── shipment.ts
│   │   ├── template.ts
│   │   └── validation.ts
│   └── stores/
│       ├── shipmentStore.ts
│       └── templateStore.ts
├── prisma/
│   └── schema.prisma
├── .env.local
└── package.json
```

---

## 三、核心实现代码

### 3.1 数据模型 (Prisma Schema)

```prisma
// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 模板映射记忆
model TemplateMapping {
  id                String    @id @default(cuid())
  templateSignature String    @unique  // 模板特征签名（列名排序后拼接）
  mappingName       String              // 映射名称，方便用户识别
  columnMapping     Json                // 列映射关系 JSON
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  useCount          Int       @default(0)  // 使用次数，用于排序推荐
}

// 运单表
model Shipment {
  id              String    @id @default(cuid())
  externalCode    String?   @map("external_code")
  senderName      String    @map("sender_name")
  senderPhone     String    @map("sender_phone")
  senderAddress   String    @map("sender_address")
  receiverName    String    @map("receiver_name")
  receiverPhone   String    @map("receiver_phone")
  receiverAddress String    @map("receiver_address")
  weight          Float              // kg，正数
  pieceCount      Int       @map("piece_count")  // 件数，正整数
  temperature     String             // 常温/冷藏/冷冻
  remark         String?
  batchId        String    @map("batch_id")     // 批次ID，同一次导入
  status         String    @default("pending") // pending/submitted/failed
  createdAt      DateTime  @default(now()) @map("created_at")
  submittedAt    DateTime? @map("submitted_at")

  @@index([externalCode])
  @@index([receiverName])
  @@index([createdAt])
  @@map("shipments")
}
```

### 3.2 类型定义

```typescript
// src/types/shipment.ts
export interface Shipment {
  id?: string;
  externalCode: string | null;
  senderName: string;
  senderPhone: string;
  senderAddress: string;
  receiverName: string;
  receiverPhone: string;
  receiverAddress: string;
  weight: number;
  pieceCount: number;
  temperature: '常温' | '冷藏' | '冷冻';
  remark: string | null;
  batchId?: string;
  rowIndex?: number;  // Excel 原始行号
}

// 字段定义
export const SHIPMENT_FIELDS = {
  externalCode: { label: '外部编码', required: false },
  senderName: { label: '发件人姓名', required: true },
  senderPhone: { label: '发件人电话', required: true },
  senderAddress: { label: '发件人地址', required: true },
  receiverName: { label: '收件人姓名', required: true },
  receiverPhone: { label: '收件人电话', required: true },
  receiverAddress: { label: '收件人地址', required: true },
  weight: { label: '重量(kg)', required: true },
  pieceCount: { label: '件数', required: true },
  temperature: { label: '温层', required: true },
  remark: { label: '备注', required: false },
} as const;

// 错误类型
export interface ValidationError {
  row: number;           // 行号（从1开始）
  field: keyof Shipment;
  errorType: 'required' | 'phone' | 'positive' | 'temperature' | 'duplicate';
  message: string;
}
```

### 3.3 校验 Schema (Zod)

```typescript
// src/lib/validation/shipmentSchema.ts
import { z } from 'zod';

// 电话正则（简单版，可根据需求调整）
const phoneRegex = /^1[3-9]\d{9}$|^0\d{2,3}-?\d{7,8}$/;

export const shipmentSchema = z.object({
  externalCode: z.string().nullable().optional(),
  senderName: z.string().min(1, '发件人姓名不能为空'),
  senderPhone: z.string().regex(phoneRegex, '电话格式错误'),
  senderAddress: z.string().min(1, '发件人地址不能为空'),
  receiverName: z.string().min(1, '收件人姓名不能为空'),
  receiverPhone: z.string().regex(phoneRegex, '电话格式错误'),
  receiverAddress: z.string().min(1, '收件人地址不能为空'),
  weight: z.number().positive('重量必须为正数'),
  pieceCount: z.number().int().positive('件数必须为正整数'),
  temperature: z.enum(['常温', '冷藏', '冷冻'], {
    errorMap: () => ({ message: '温层必须是：常温/冷藏/冷冻' })
  }),
  remark: z.string().nullable().optional(),
});

export type ShipmentInput = z.infer<typeof shipmentSchema>;

// 批量校验函数
export function validateShipments(
  shipments: Shipment[], 
  existingExternalCodes: Set<string> = new Set()
): Map<number, ValidationError[]> {
  const errors = new Map<number, ValidationError[]>();
  const batchExternalCodes = new Set<string>();
  
  shipments.forEach((shipment, idx) => {
    const rowErrors: ValidationError[] = [];
    const rowNum = shipment.rowIndex || idx + 1;
    
    // 检查外部编码重复（批次内）
    if (shipment.externalCode) {
      if (batchExternalCodes.has(shipment.externalCode)) {
        rowErrors.push({
          row: rowNum,
          field: 'externalCode',
          errorType: 'duplicate',
          message: `外部编码"${shipment.externalCode}"与本批次第${findRowByCode(shipments, shipment.externalCode)}行重复`
        });
      } else if (existingExternalCodes.has(shipment.externalCode)) {
        rowErrors.push({
          row: rowNum,
          field: 'externalCode',
          errorType: 'duplicate',
          message: `外部编码"${shipment.externalCode}"与历史数据重复`
        });
      }
      batchExternalCodes.add(shipment.externalCode);
    }
    
    // 使用 Zod 校验
    const result = shipmentSchema.safeParse(shipment);
    if (!result.success) {
      result.error.errors.forEach(err => {
        const field = err.path[0] as keyof Shipment;
        rowErrors.push({
          row: rowNum,
          field,
          errorType: 'required',
          message: err.message
        });
      });
    }
    
    if (rowErrors.length > 0) {
      errors.set(rowNum, rowErrors);
    }
  });
  
  return errors;
}
```

### 3.4 模板识别与映射

```typescript
// src/lib/excel/templateDetector.ts
import { read, utils } from 'xlsx';

// 标准字段及其可能的别名
const FIELD_ALIASES: Record<keyof Shipment, string[]> = {
  externalCode: ['外部编码', '外部单号', '客户单号', '外部订单号', '订单号', '外单号'],
  senderName: ['发件人姓名', '寄件人', '发件人', '寄件人姓名', '发货人', '发货人姓名'],
  senderPhone: ['发件人电话', '寄件人电话', '发件人手机', '寄件人手机', '发货人电话'],
  senderAddress: ['发件人地址', '寄件人地址', '发件地址', '寄件地址', '发货地址'],
  receiverName: ['收件人姓名', '收件人', '收货人', '收货人姓名', '收件人名称'],
  receiverPhone: ['收件人电话', '收货人电话', '收件人手机', '收货人手机', '联系电话'],
  receiverAddress: ['收件人地址', '收货人地址', '收件地址', '收货地址'],
  weight: ['重量', '重量(kg)', '货物重量', '包裹重量', 'KG', '千克'],
  pieceCount: ['件数', '数量', '包裹数量', '箱数', '件数(箱)'],
  temperature: ['温层', '温度', '温区', '温层要求', '储存温度', '温控'],
  remark: ['备注', '说明', '附言', '备注说明', '特殊要求']
};

// 生成模板特征签名
function generateTemplateSignature(headers: string[]): string {
  // 标准化列名（去空格、转小写）
  const normalized = headers
    .map(h => h?.trim().toLowerCase().replace(/[（(].*[）)]/g, ''))
    .filter(Boolean)
    .sort()
    .join('|');
  return normalized;
}

// 自动检测列映射
export function detectColumnMapping(
  headers: string[]
): Partial<Record<keyof Shipment, string>> {
  const mapping: Partial<Record<keyof Shipment, string>> = {};
  
  for (const [field, aliases] of Object.entries(FIELD_ALIASES)) {
    for (const header of headers) {
      const normalizedHeader = header?.trim().toLowerCase();
      if (aliases.some(alias => 
        normalizedHeader === alias.toLowerCase() ||
        normalizedHeader.includes(alias.toLowerCase())
      )) {
        mapping[field as keyof Shipment] = header;
        break;
      }
    }
  }
  
  return mapping;
}

// 保存模板映射
export async function saveTemplateMapping(
  headers: string[],
  columnMapping: Record<string, string>,
  mappingName: string
): Promise<void> {
  const signature = generateTemplateSignature(headers);
  
  await fetch('/api/templates', {
    method: 'POST',
    body: JSON.stringify({
      signature,
      mappingName,
      columnMapping
    })
  });
}

// 获取匹配的模板映射
export async function getMatchingTemplate(
  headers: string[]
): Promise<Record<string, string> | null> {
  const signature = generateTemplateSignature(headers);
  
  const response = await fetch(`/api/templates?signature=${signature}`);
  const data = await response.json();
  
  return data.columnMapping || null;
}
```

### 3.5 Excel 解析与导入

```typescript
// src/lib/excel/parser.ts
import { read, utils } from 'xlsx';

export interface ParseResult {
  data: Shipment[];
  headers: string[];
  totalRows: number;
  errors: string[];
}

export async function parseExcelFile(
  file: File,
  columnMapping?: Record<string, string>
): Promise<ParseResult> {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    
    reader.onload = (e) => {
      try {
        const data = new Uint8Array(e.target?.result as ArrayBuffer);
        const workbook = read(data, { type: 'array' });
        
        if (!workbook.SheetNames.length) {
          reject(new Error('Excel 文件中没有找到工作表'));
          return;
        }
        
        const sheet = workbook.Sheets[workbook.SheetNames[0]];
        const rawData = utils.sheet_to_json<any[]>(sheet, { header: 1, defval: '' });
        
        if (!rawData || rawData.length < 2) {
          reject(new Error('文件为空或只有表头行'));
          return;
        }
        
        const headers = rawData[0].map((h: any) => h?.toString().trim() || '');
        const rows = rawData.slice(1);
        
        // 应用列映射
        const shipments = mapRowsToShipments(rows, headers, columnMapping);
        
        resolve({
          data: shipments,
          headers,
          totalRows: rows.length,
          errors: []
        });
      } catch (error) {
        reject(new Error('文件解析失败：' + (error as Error).message));
      }
    };
    
    reader.onerror = () => reject(new Error('文件读取失败'));
    reader.readAsArrayBuffer(file);
  });
}

function mapRowsToShipments(
  rows: any[][],
  headers: string[],
  columnMapping?: Record<string, string>
): Shipment[] {
  // 创建列索引映射
  const indexMap: Record<string, number> = {};
  
  if (columnMapping) {
    // 使用用户映射
    for (const [field, headerName] of Object.entries(columnMapping)) {
      const idx = headers.findIndex(h => h === headerName);
      if (idx !== -1) indexMap[field] = idx;
    }
  } else {
    // 自动检测映射
    const autoMapping = detectColumnMapping(headers);
    for (const [field, headerName] of Object.entries(autoMapping)) {
      const idx = headers.findIndex(h => h === headerName);
      if (idx !== -1) indexMap[field] = idx;
    }
  }
  
  return rows.map((row, idx) => ({
    externalCode: indexMap.externalCode ? row[indexMap.externalCode]?.toString() || null : null,
    senderName: indexMap.senderName ? row[indexMap.senderName]?.toString() || '' : '',
    senderPhone: indexMap.senderPhone ? row[indexMap.senderPhone]?.toString() || '' : '',
    senderAddress: indexMap.senderAddress ? row[indexMap.senderAddress]?.toString() || '' : '',
    receiverName: indexMap.receiverName ? row[indexMap.receiverName]?.toString() || '' : '',
    receiverPhone: indexMap.receiverPhone ? row[indexMap.receiverPhone]?.toString() || '' : '',
    receiverAddress: indexMap.receiverAddress ? row[indexMap.receiverAddress]?.toString() || '' : '',
    weight: parseFloat(row[indexMap.weight]) || 0,
    pieceCount: parseInt(row[indexMap.pieceCount]) || 0,
    temperature: row[indexMap.temperature]?.toString() || '',
    remark: indexMap.remark ? row[indexMap.remark]?.toString() || null : null,
    rowIndex: idx + 1
  }));
}
```

### 3.6 可编辑表格组件

```tsx
// src/components/preview/EditableTable.tsx
'use client';

import { useState, useCallback, useMemo } from 'react';
import { useReactTable, getCoreRowModel, flexRender } from '@tanstack/react-table';
import { validateShipments } from '@/lib/validation/shipmentSchema';
import { ErrorTooltip } from './ErrorTooltip';
import { ValidationSummary } from './ValidationSummary';

interface EditableTableProps {
  data: Shipment[];
  onDataChange: (data: Shipment[]) => void;
  existingCodes?: Set<string>;
}

export function EditableTable({ data, onDataChange, existingCodes = new Set() }: EditableTableProps) {
  const [validationErrors, setValidationErrors] = useState<Map<number, ValidationError[]>>(new Map());
  
  // 实时校验
  const validateData = useCallback((shipments: Shipment[]) => {
    const errors = validateShipments(shipments, existingCodes);
    setValidationErrors(errors);
    return errors;
  }, [existingCodes]);
  
  // 更新单元格
  const updateCell = useCallback((rowIndex: number, field: keyof Shipment, value: any) => {
    const newData = [...data];
    newData[rowIndex] = { ...newData[rowIndex], [field]: value };
    onDataChange(newData);
    validateData(newData);
  }, [data, onDataChange, validateData]);
  
  // 删除行
  const deleteRow = useCallback((rowIndex: number) => {
    const newData = data.filter((_, idx) => idx !== rowIndex);
    onDataChange(newData);
    validateData(newData);
  }, [data, onDataChange, validateData]);
  
  // 新增空行
  const addRow = useCallback(() => {
    const emptyRow: Shipment = {
      externalCode: null,
      senderName: '',
      senderPhone: '',
      senderAddress: '',
      receiverName: '',
      receiverPhone: '',
      receiverAddress: '',
      weight: 0,
      pieceCount: 1,
      temperature: '常温',
      remark: null,
      rowIndex: data.length + 1
    };
    onDataChange([...data, emptyRow]);
  }, [data, onDataChange]);
  
  const hasErrors = validationErrors.size > 0;
  const errorCount = Array.from(validationErrors.values()).reduce((acc, errs) => acc + errs.length, 0);
  
  return (
    <div className="flex flex-col h-full">
      {/* 错误汇总 */}
      <ValidationSummary 
        errors={validationErrors} 
        onRowClick={(rowNum) => {
          // 滚动到对应行
          document.getElementById(`row-${rowNum}`)?.scrollIntoView({ behavior: 'smooth' });
        }}
      />
      
      {/* 工具栏 */}
      <div className="flex justify-between items-center p-4 border-b">
        <div className="text-sm text-gray-600">
          共 {data.length} 条数据
          {hasErrors && <span className="text-red-500 ml-2">（{errorCount} 个错误）</span>}
        </div>
        <div className="flex gap-2">
          <button onClick={addRow} className="px-3 py-1 bg-blue-500 text-white rounded">
            + 新增行
          </button>
          <button onClick={() => exportToExcel(data)} className="px-3 py-1 border rounded">
            导出 Excel
          </button>
        </div>
      </div>
      
      {/* 表格容器 */}
      <div className="flex-1 overflow-auto">
        <table className="min-w-full border-collapse">
          <thead className="sticky top-0 bg-gray-100 z-10">
            <tr>
              <th className="border p-2 w-12">#</th>
              <th className="border p-2">外部编码</th>
              <th className="border p-2">发件人姓名*</th>
              <th className="border p-2">发件人电话*</th>
              <th className="border p-2">发件人地址*</th>
              <th className="border p-2">收件人姓名*</th>
              <th className="border p-2">收件人电话*</th>
              <th className="border p-2">收件人地址*</th>
              <th className="border p-2">重量(kg)*</th>
              <th className="border p-2">件数*</th>
              <th className="border p-2">温层*</th>
              <th className="border p-2">备注</th>
              <th className="border p-2 w-12">操作</th>
            </tr>
          </thead>
          <tbody>
            {data.map((row, idx) => {
              const rowErrors = validationErrors.get(row.rowIndex || idx + 1);
              const hasRowError = rowErrors && rowErrors.length > 0;
              
              return (
                <tr 
                  key={idx} 
                  id={`row-${row.rowIndex || idx + 1}`}
                  className={hasRowError ? 'bg-red-50' : ''}
                >
                  <td className="border p-2 text-center">{idx + 1}</td>
                  
                  {/* 外部编码 */}
                  <td className="border p-2">
                    <ErrorTooltip error={getFieldError(rowErrors, 'externalCode')}>
                      <input
                        value={row.externalCode || ''}
                        onChange={(e) => updateCell(idx, 'externalCode', e.target.value || null)}
                        className="w-full p-1 border rounded focus:outline-none focus:ring-2 focus:ring-blue-300"
                      />
                    </ErrorTooltip>
                  </td>
                  
                  {/* 发件人姓名 */}
                  <td className="border p-2">
                    <ErrorTooltip error={getFieldError(rowErrors, 'senderName')}>
                      <input
                        value={row.senderName}
                        onChange={(e) => updateCell(idx, 'senderName', e.target.value)}
                        className={`w-full p-1 border rounded ${getFieldError(rowErrors, 'senderName') ? 'border-red-500 bg-red-50' : ''}`}
                      />
                    </ErrorTooltip>
                  </td>
                  
                  {/* 发件人电话 */}
                  <td className="border p-2">
                    <ErrorTooltip error={getFieldError(rowErrors, 'senderPhone')}>
                      <input
                        value={row.senderPhone}
                        onChange={(e) => updateCell(idx, 'senderPhone', e.target.value)}
                        className={`w-full p-1 border rounded ${getFieldError(rowErrors, 'senderPhone') ? 'border-red-500 bg-red-50' : ''}`}
                      />
                    </ErrorTooltip>
                  </td>
                  
                  {/* 地址字段类似，省略... */}
                  
                  {/* 重量 */}
                  <td className="border p-2">
                    <ErrorTooltip error={getFieldError(rowErrors, 'weight')}>
                      <input
                        type="number"
                        step="0.01"
                        value={row.weight}
                        onChange={(e) => updateCell(idx, 'weight', parseFloat(e.target.value) || 0)}
                        className={`w-full p-1 border rounded ${getFieldError(rowErrors, 'weight') ? 'border-red-500 bg-red-50' : ''}`}
                      />
                    </ErrorTooltip>
                  </td>
                  
                  {/* 件数 */}
                  <td className="border p-2">
                    <ErrorTooltip error={getFieldError(rowErrors, 'pieceCount')}>
                      <input
                        type="number"
                        step="1"
                        value={row.pieceCount}
                        onChange={(e) => updateCell(idx, 'pieceCount', parseInt(e.target.value) || 0)}
                        className={`w-full p-1 border rounded ${getFieldError(rowErrors, 'pieceCount') ? 'border-red-500 bg-red-50' : ''}`}
                      />
                    </ErrorTooltip>
                  </td>
                  
                  {/* 温层 */}
                  <td className="border p-2">
                    <ErrorTooltip error={getFieldError(rowErrors, 'temperature')}>
                      <select
                        value={row.temperature}
                        onChange={(e) => updateCell(idx, 'temperature', e.target.value as any)}
                        className={`w-full p-1 border rounded ${getFieldError(rowErrors, 'temperature') ? 'border-red-500 bg-red-50' : ''}`}
                      >
                        <option value="常温">常温</option>
                        <option value="冷藏">冷藏</option>
                        <option value="冷冻">冷冻</option>
                      </select>
                    </ErrorTooltip>
                  </td>
                  
                  {/* 备注 */}
                  <td className="border p-2">
                    <input
                      value={row.remark || ''}
                      onChange={(e) => updateCell(idx, 'remark', e.target.value || null)}
                      className="w-full p-1 border rounded"
                    />
                  </td>
                  
                  <td className="border p-2 text-center">
                    <button onClick={() => deleteRow(idx)} className="text-red-500 hover:text-red-700">
                      删除
                    </button>
                  </td>
                </tr>
              );
            })}
          </tbody>
        </table>
      </div>
    </div>
  );
}

function getFieldError(errors: ValidationError[] | undefined, field: string): string | null {
  if (!errors) return null;
  const error = errors.find(e => e.field === field);
  return error?.message || null;
}
```

### 3.7 文件上传组件

```tsx
// src/components/upload/DragDropUpload.tsx
'use client';

import { useCallback, useState } from 'react';
import { useDropzone } from 'react-dropzone';
import { parseExcelFile, detectColumnMapping } from '@/lib/excel/parser';
import { getMatchingTemplate, saveTemplateMapping } from '@/lib/excel/templateDetector';
import { ImportProgress } from './ImportProgress';
import { TemplateMappingDialog } from './TemplateMappingDialog';

interface DragDropUploadProps {
  onImportComplete: (data: Shipment[], headers: string[]) => void;
}

export function DragDropUpload({ onImportComplete }: DragDropUploadProps) {
  const [isLoading, setIsLoading] = useState(false);
  const [progress, setProgress] = useState(0);
  const [currentRow, setCurrentRow] = useState(0);
  const [totalRows, setTotalRows] = useState(0);
  const [showMappingDialog, setShowMappingDialog] = useState(false);
  const [pendingParseResult, setPendingParseResult] = useState<any>(null);
  
  const onDrop = useCallback(async (acceptedFiles: File[]) => {
    const file = acceptedFiles[0];
    if (!file) return;
    
    // 文件类型校验
    const ext = file.name.split('.').pop()?.toLowerCase();
    if (!['xlsx', 'xls'].includes(ext || '')) {
      alert('仅支持 .xlsx 或 .xls 格式的文件');
      return;
    }
    
    setIsLoading(true);
    setProgress(10);
    
    try {
      // 解析文件
      setProgress(30);
      const result = await parseExcelFile(file);
      setTotalRows(result.totalRows);
      setCurrentRow(0);
      
      // 模拟进度更新
      const interval = setInterval(() => {
        setCurrentRow(prev => {
          const newVal = Math.min(prev + Math.ceil(result.totalRows / 50), result.totalRows);
          setProgress(30 + (newVal / result.totalRows) * 60);
          return newVal;
        });
      }, 50);
      
      // 检测模板匹配
      setProgress(90);
      const existingMapping = await getMatchingTemplate(result.headers);
      
      clearInterval(interval);
      setProgress(100);
      
      if (existingMapping && Object.keys(existingMapping).length > 0) {
        // 应用已有映射，重新解析
        const mappedResult = await parseExcelFile(file, existingMapping);
        onImportComplete(mappedResult.data, mappedResult.headers);
      } else {
        // 显示映射配置对话框
        const autoMapping = detectColumnMapping(result.headers);
        setPendingParseResult({ file, headers: result.headers, autoMapping });
        setShowMappingDialog(true);
      }
      
    } catch (error) {
      alert('文件解析失败：' + (error as Error).message);
    } finally {
      setIsLoading(false);
      setProgress(0);
    }
  }, [onImportComplete]);
  
  const { getRootProps, getInputProps, isDragActive } = useDropzone({
    onDrop,
    accept: {
      'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet': ['.xlsx'],
      'application/vnd.ms-excel': ['.xls']
    },
    maxFiles: 1
  });
  
  return (
    <div className="w-full">
      <div
        {...getRootProps()}
        className={`
          border-2 border-dashed rounded-lg p-12 text-center cursor-pointer
          transition-colors duration-200
          ${isDragActive ? 'border-blue-500 bg-blue-50' : 'border-gray-300 hover:border-blue-400'}
        `}
      >
        <input {...getInputProps()} />
        <div className="text-gray-600">
          {isDragActive ? (
            <p>释放文件以上传</p>
          ) : (
            <>
              <p className="text-lg">拖拽 Excel 文件到此处，或点击选择文件</p>
              <p className="text-sm mt-2">支持 .xlsx 和 .xls 格式，单次最多 1000 条数据</p>
            </>
          )}
        </div>
      </div>
      
      <ImportProgress
        progress={progress}
        currentRow={currentRow}
        totalRows={totalRows}
        visible={isLoading}
      />
      
      <TemplateMappingDialog
        open={showMappingDialog}
        headers={pendingParseResult?.headers || []}
        autoMapping={pendingParseResult?.autoMapping || {}}
        onConfirm={async (columnMapping, mappingName) => {
          // 保存映射并重新解析
          await saveTemplateMapping(
            pendingParseResult.headers,
            columnMapping,
            mappingName
          );
          const result = await parseExcelFile(pendingParseResult.file, columnMapping);
          onImportComplete(result.data, result.headers);
          setShowMappingDialog(false);
        }}
        onCancel={() => setShowMappingDialog(false)}
      />
    </div>
  );
}
```

### 3.8 API 路由

```typescript
// src/app/api/shipments/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { prisma } from '@/lib/db/client';
import { shipmentSchema } from '@/lib/validation/shipmentSchema';

// 提交运单
export async function POST(request: NextRequest) {
  try {
    const { shipments, batchId } = await request.json();
    
    const results = {
      success: 0,
      failed: 0,
      errors: [] as any[]
    };
    
    for (const shipment of shipments) {
      try {
        const validated = shipmentSchema.parse(shipment);
        
        await prisma.shipment.create({
          data: {
            externalCode: validated.externalCode,
            senderName: validated.senderName,
            senderPhone: validated.senderPhone,
            senderAddress: validated.senderAddress,
            receiverName: validated.receiverName,
            receiverPhone: validated.receiverPhone,
            receiverAddress: validated.receiverAddress,
            weight: validated.weight,
            pieceCount: validated.pieceCount,
            temperature: validated.temperature,
            remark: validated.remark,
            batchId,
            status: 'submitted',
            submittedAt: new Date()
          }
        });
        
        results.success++;
      } catch (error) {
        results.failed++;
        results.errors.push({
          externalCode: shipment.externalCode,
          error: (error as Error).message
        });
      }
    }
    
    return NextResponse.json(results);
  } catch (error) {
    return NextResponse.json(
      { error: '提交失败：' + (error as Error).message },
      { status: 500 }
    );
  }
}

// 查询历史运单
export async function GET(request: NextRequest) {
  const searchParams = request.nextUrl.searchParams;
  const externalCode = searchParams.get('externalCode');
  const receiverName = searchParams.get('receiverName');
  const startDate = searchParams.get('startDate');
  const endDate = searchParams.get('endDate');
  const page = parseInt(searchParams.get('page') || '1');
  const pageSize = parseInt(searchParams.get('pageSize') || '20');
  
  const where: any = {};
  
  if (externalCode) {
    where.externalCode = { contains: externalCode, mode: 'insensitive' };
  }
  if (receiverName) {
    where.receiverName = { contains: receiverName, mode: 'insensitive' };
  }
  if (startDate || endDate) {
    where.createdAt = {};
    if (startDate) where.createdAt.gte = new Date(startDate);
    if (endDate) where.createdAt.lte = new Date(endDate);
  }
  
  const [shipments, total] = await Promise.all([
    prisma.shipment.findMany({
      where,
      orderBy: { createdAt: 'desc' },
      skip: (page - 1) * pageSize,
      take: pageSize
    }),
    prisma.shipment.count({ where })
  ]);
  
  return NextResponse.json({
    data: shipments,
    total,
    page,
    pageSize,
    totalPages: Math.ceil(total / pageSize)
  });
}
```

### 3.9 模板映射 API

```typescript
// src/app/api/templates/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { prisma } from '@/lib/db/client';

export async function GET(request: NextRequest) {
  const signature = request.nextUrl.searchParams.get('signature');
  
  if (signature) {
    const template = await prisma.templateMapping.findUnique({
      where: { templateSignature: signature }
    });
    return NextResponse.json(template);
  }
  
  const templates = await prisma.templateMapping.findMany({
    orderBy: { useCount: 'desc' }
  });
  return NextResponse.json(templates);
}

export async function POST(request: NextRequest) {
  const { signature, mappingName, columnMapping } = await request.json();
  
  const template = await prisma.templateMapping.upsert({
    where: { templateSignature: signature },
    update: {
      columnMapping,
      useCount: { increment: 1 },
      updatedAt: new Date()
    },
    create: {
      templateSignature: signature,
      mappingName,
      columnMapping
    }
  });
  
  return NextResponse.json(template);
}
```

---

## 四、关键功能实现要点

### 4.1 进度条实现（支持千条数据）

```tsx
// 使用 React 虚拟滚动处理大数据量
import { useVirtualizer } from '@tanstack/react-virtual';

// 在表格组件中添加虚拟滚动
const parentRef = useRef<HTMLDivElement>(null);
const rowVirtualizer = useVirtualizer({
  count: data.length,
  getScrollElement: () => parentRef.current,
  estimateSize: () => 48,
  overscan: 10
});
```

### 4.2 错误一次性展示

```tsx
// ValidationSummary 组件 - 汇总所有错误
export function ValidationSummary({ errors, onRowClick }: { 
  errors: Map<number, ValidationError[]>;
  onRowClick: (rowNum: number) => void;
}) {
  if (errors.size === 0) return null;
  
  const allErrors = Array.from(errors.entries()).flatMap(([row, errs]) =>
    errs.map(err => ({ row, ...err }))
  );
  
  return (
    <div className="bg-red-50 border border-red-200 rounded-lg p-4 mb-4">
      <h4 className="text-red-800 font-semibold mb-2">
        发现 {errors.size} 行数据存在 {allErrors.length} 个错误：
      </h4>
      <ul className="list-disc list-inside space-y-1 max-h-48 overflow-auto">
        {allErrors.map((err, idx) => (
          <li key={idx} className="text-sm text-red-700">
            <button 
              onClick={() => onRowClick(err.row)}
              className="hover:underline cursor-pointer"
            >
              第 {err.row} 行，{err.field}：{err.message}
            </button>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

### 4.3 提交下单

```tsx
// src/hooks/useBatchSubmit.ts
export function useBatchSubmit() {
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [submitProgress, setSubmitProgress] = useState(0);
  
  const submitBatch = useCallback(async (shipments: Shipment[]) => {
    setIsSubmitting(true);
    const batchId = crypto.randomUUID();
    
    // 分批提交，每批 100 条
    const batchSize = 100;
    const batches = [];
    for (let i = 0; i < shipments.length; i += batchSize) {
      batches.push(shipments.slice(i, i + batchSize));
    }
    
    let successCount = 0;
    let failCount = 0;
    const errors = [];
    
    for (let i = 0; i < batches.length; i++) {
      const result = await fetch('/api/shipments', {
        method: 'POST',
        body: JSON.stringify({ shipments: batches[i], batchId })
      });
      const data = await result.json();
      successCount += data.success;
      failCount += data.failed;
      errors.push(...data.errors);
      setSubmitProgress(((i + 1) / batches.length) * 100);
    }
    
    setIsSubmitting(false);
    return { success: successCount, failed: failCount, errors };
  }, []);
  
  return { submitBatch, isSubmitting, submitProgress };
}
```

---

## 五、数据库连接配置

```env
# .env.local
DATABASE_URL="postgresql://user:password@localhost:5432/logistics"
# 或 Supabase
NEXT_PUBLIC_SUPABASE_URL="your-url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-key"
```

```typescript
// src/lib/db/client.ts
import { PrismaClient } from '@prisma/client';

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma = globalForPrisma.prisma ?? new PrismaClient();

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;
```

---

## 六、部署到 Vercel

```bash
# 1. 安装依赖
npm install

# 2. 初始化数据库（以 Neon 为例）
npx prisma db push
npx prisma generate

# 3. 构建
npm run build

# 4. 部署到 Vercel
vercel --prod

# 环境变量需要在 Vercel Dashboard 中配置
# - DATABASE_URL
```

### Vercel 部署配置 (vercel.json)

```json
{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
```

---

## 七、使用说明

1. **启动开发环境**
```bash
npm run dev
# 访问 http://localhost:3000
```

2. **上传文件流程**
   - 拖拽或点击上传 Excel
   - 自动识别模板或手动配置映射
   - 系统自动记忆映射关系

3. **预览编辑**
   - 直接点击单元格编辑
   - 实时校验，行内显示错误
   - 错误汇总面板一次性展示所有问题

4. **提交下单**
   - 无错误行方可提交
   - 显示提交进度
   - 返回成功/失败统计

5. **历史查询**
   - 按外部编码、收件人姓名、时间筛选
   - 分页展示历史运单

---

## 八、注意事项

1. **大数据量处理**：使用虚拟滚动（TanStack Virtual）处理 1000+ 条数据
2. **文件解析**：使用 Web Worker 处理大文件解析，避免 UI 卡顿
3. **错误处理**：所有 API 调用需有完整 error handling
4. **验证规则**：电话正则可根据实际需求调整
5. **安全性**：添加 API 限流和文件大小限制（建议 10MB）
```

---

这个 Skill 包含了完整的技术方案、代码实现和部署指南。你可以将它导入到 IDE 的 AI 助手中，按照指导逐步实现整个系统。需要我详细解释某个模块的实现细节吗？